// إعدادات البيئة الافتراضية (تُستخدم في builds الإنتاج).
// عدّل apiUrl ليشير إلى عنوان الـ API في بيئة الإنتاج.
export const environment = {
  production: true,
  apiUrl: 'https://localhost:44330/api'
};
