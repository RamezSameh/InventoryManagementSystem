import {Component,inject} from '@angular/core'; import {CommonModule} from '@angular/common'; import {FormBuilder,ReactiveFormsModule,Validators} from '@angular/forms'; import {ApiService} from '../core/api.service'; @Component({standalone:true,imports:[CommonModule,ReactiveFormsModule],template:`<div class="page-head"><div><h2>حركة المخزون</h2><p>إضافة ومتابعة عمليات دخول وخروج المنتجات</p></div></div><div class="stock-layout"><div class="panel form-panel"><h3>إضافة حركة جديدة</h3><p class="muted">سجّل عملية شراء أو بيع أو تسوية للمخزون</p><form [formGroup]="form" (ngSubmit)="submit()"><label>رقم المنتج<input type="number" formControlName="productId" placeholder="مثال: 1"></label><label>رقم المستودع<input type="number" formControlName="warehouseId" placeholder="مثال: 1"></label><label>نوع الحركة<select formControlName="type"><option [ngValue]="1">شراء - إدخال</option><option [ngValue]="2">بيع - إخراج</option><option [ngValue]="5">تسوية</option><option [ngValue]="6">مرتجع</option></select></label><label>الكمية<input type="number" formControlName="quantity" min="1"></label><label>المرجع <span class="optional">اختياري</span><input formControlName="reference" placeholder="رقم الفاتورة أو المرجع"></label><button class="primary full" [disabled]="form.invalid">حفظ الحركة</button><p class="success" *ngIf="success">تم حفظ الحركة بنجاح</p><p class="error" *ngIf="error">تعذر حفظ الحركة</p></form></div><div class="panel"><div class="panel-head"><div><h3>منتجات منخفضة المخزون</h3><p>تحتاج إلى إعادة طلب</p></div><span class="alert-count">{{low.length}}</span></div><div class="alert-item" *ngFor="let x of low"><div class="alert-icon">!</div><div><b>{{x.productName}}</b><small>{{x.warehouseName}} · متبقي {{x.quantity}}</small></div></div><div class="empty" *ngIf="!low.length">المخزون ضمن المستويات الطبيعية</div></div></div>`}) export class StockComponent{private fb=inject(FormBuilder);api=inject(ApiService);low:any[]=[];success=false;error=false;form=this.fb.nonNullable.group({productId:[1,Validators.required],warehouseId:[1,Validators.required],type:[1,Validators.required],quantity:[1,[Validators.required,Validators.min(1)]],reference:['']});ngOnInit(){this.api.lowStock().subscribe(x=>this.low=x)}submit(){this.success=false;this.error=false;this.api.addMovement(this.form.getRawValue()).subscribe({next:()=>{this.success=true;this.form.patchValue({quantity:1,reference:''});this.api.lowStock().subscribe(x=>this.low=x)},error:()=>this.error=true})}}