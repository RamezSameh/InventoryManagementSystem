import { Component, inject } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { Router } from '@angular/router';
import { CommonModule } from '@angular/common'; 

import { ApiService } from '../core/api.service'; @Component({ standalone: true, imports: [ReactiveFormsModule, CommonModule], template: `<div class="login-page"><div class="login-art"><div class="art-content"><span class="brand-mark large">▦</span><h1>إدارة مخزونك<br><em>بذكاء وبساطة</em></h1><p>كل ما تحتاجه لإدارة منتجاتك، مستودعاتك وتقاريرك في مكان واحد.</p><div class="art-grid"><span>▤ منتجاتك منظمة</span><span>↗ تقارير فورية</span><span>⌁ مخزون دقيق</span><span>✓ قرارات أذكى</span></div></div></div><div class="login-form"><div class="form-box"><div class="mobile-logo"><span class="brand-mark">▦</span><b>مخزني</b></div><h2>تسجيل الدخول</h2><p class="muted">أدخل بياناتك للوصول إلى لوحة التحكم</p><form [formGroup]="form" (ngSubmit)="submit()"><label>البريد الإلكتروني<input type="email" formControlName="email" placeholder="admin&#64;inventory.com"></label><label>كلمة المرور<input type="password" formControlName="password" placeholder="••••••••"></label><button class="primary full" [disabled]="form.invalid||loading" style="margin-top:25px">{{loading?'جارٍ الدخول...':'دخول إلى النظام'}}</button><p class="error" *ngIf="error">{{error}}</p></form><p class="demo">الحساب التجريبي مفعّل مسبقاً في بيئة التطوير</p></div></div></div>` }) export class LoginComponent { private fb = inject(FormBuilder); private api = inject(ApiService); private router = inject(Router); form = this.fb.nonNullable.group({ email: ['admin&#64;inventory.com', [Validators.required, Validators.email]], password: ['Admin@123', Validators.required] }); loading = false; error = ''; submit() { if (this.form.invalid) return; this.loading = true; this.api.login(this.form.value.email!, this.form.value.password!).subscribe({ next: r => { localStorage.setItem('inventory_token', r.token); this.router.navigateByUrl('/') }, error: () => { this.error = 'تعذر تسجيل الدخول. تحقق من البيانات واتصال الخادم.'; this.loading = false } }) } }