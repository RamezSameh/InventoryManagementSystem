import { Injectable,inject } from '@angular/core'; import { HttpClient } from '@angular/common/http'; import { Observable } from 'rxjs'; export interface Product{ id:number; name:string; sku:string; barcode?:string; purchasePrice:number; sellingPrice:number; minimumStock:number; categoryName:string; totalStock:number } export interface LoginResponse{token:string} @Injectable({providedIn:'root'}) export class ApiService{private http=inject(HttpClient); private base='https://localhost:44330/api'; login(email:string,password:string){return this.http.post<LoginResponse>(`${this.base}/auth/login`,{email,password})} products():Observable<Product[]>{return this.http.get<Product[]>(`${this.base}/products`)} lowStock(){return this.http.get<any[]>(`${this.base}/stock/low-stock`)} addMovement(payload:any){return this.http.post(`${this.base}/stock/movement`,payload)} }
